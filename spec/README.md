# spec/

本目录仅保留 golden vectors（`vectors/*.json`），供 `core/` 的 `*_vectors.rs` 测试消费。

协议规格文档（字节级权威）已迁移至 wiki：
[protocol/](https://github.com/welyin/spark/wiki/protocol/)（identity / sync-evidence / p2p-messages / org / data-mgmt）
