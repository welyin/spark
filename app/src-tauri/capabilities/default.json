{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力集（核心事件/窗口 API + 保存对话框 + 打开文件对话框（.spkg 侧载导入）；业务全部走自定义 command）",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-save", "dialog:allow-open"]
}
